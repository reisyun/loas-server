# Migration `20210211125458-initial`

This migration has been generated by reisyun at 2/11/2021, 9:55:00 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "feature"."Gender" AS ENUM ('SECRET', 'MALE', 'FEMALE')

CREATE TYPE "feature"."Language" AS ENUM ('KOREAN', 'ENGLISH', 'JAPANESE')

CREATE TYPE "feature"."HistoryStatus" AS ENUM ('COMPLETED', 'CURRENT', 'PLANNING')

CREATE TYPE "feature"."MediaStatus" AS ENUM ('FINISHED', 'RELEASING', 'UNRELEASED', 'CANCELLED', 'HIATUS')

CREATE TABLE "user" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "password" TEXT NOT NULL,
    "gender" "Gender" NOT NULL,
    "language" "Language" NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "removedAt" TIMESTAMP(3),

    PRIMARY KEY ("id")
)

CREATE TABLE "history" (
    "status" "HistoryStatus" NOT NULL,
    "repeat" INTEGER DEFAULT 0,
    "secret" BOOLEAN DEFAULT false,
    "completedAt" TIMESTAMP(3) DEFAULT CURRENT_TIMESTAMP,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT NOT NULL,
    "mediaId" TEXT NOT NULL,

    PRIMARY KEY ("userId","mediaId")
)

CREATE TABLE "media" (
    "id" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "synopsis" TEXT,
    "status" "MediaStatus" NOT NULL,
    "coverImage" TEXT,
    "bannerImage" TEXT,
    "startDate" TIMESTAMP(3),
    "endDate" TIMESTAMP(3),
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "removedAt" TIMESTAMP(3),

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "user.name_unique" ON "user"("name")

CREATE UNIQUE INDEX "user.email_unique" ON "user"("email")

ALTER TABLE "history" ADD FOREIGN KEY("userId")REFERENCES "user"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "history" ADD FOREIGN KEY("mediaId")REFERENCES "media"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20210211125458-initial
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,94 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+// User
+
+enum Gender {
+  SECRET
+  MALE
+  FEMALE
+}
+
+enum Language {
+  KOREAN
+  ENGLISH
+  JAPANESE
+}
+
+model User {
+  id       String   @id @default(uuid())
+  name     String   @unique
+  email    String   @unique
+  password String
+  gender   Gender
+  language Language
+
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  removedAt DateTime?
+
+  histories History[]
+
+  @@map(name: "user")
+}
+
+// History
+
+enum HistoryStatus {
+  COMPLETED
+  CURRENT
+  PLANNING
+}
+
+model History {
+  status      HistoryStatus
+  repeat      Int?          @default(0)
+  secret      Boolean?      @default(false)
+  completedAt DateTime?     @default(now())
+
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+
+  user    User   @relation(fields: [userId], references: [id])
+  userId  String
+  media   Media  @relation(fields: [mediaId], references: [id])
+  mediaId String
+
+  @@id([userId, mediaId])
+  @@map(name: "history")
+}
+
+// Media
+
+enum MediaStatus {
+  FINISHED
+  RELEASING
+  UNRELEASED
+  CANCELLED
+  HIATUS
+}
+
+model Media {
+  id          String      @id @default(uuid())
+  title       String
+  synopsis    String?
+  status      MediaStatus
+  coverImage  String?
+  bannerImage String?
+  startDate   DateTime?
+  endDate     DateTime?
+
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  removedAt DateTime?
+
+  histories History[]
+
+  @@map(name: "media")
+}
```


